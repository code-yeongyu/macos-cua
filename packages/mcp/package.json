{
	"name": "@macos-cua/mcp",
	"version": "0.1.0",
	"type": "module",
	"description": "Model Context Protocol server for macOS computer control",
	"bin": {
		"macos-cua-mcp": "./dist/server.js"
	},
	"scripts": {
		"build": "tsgo",
		"typecheck": "tsgo --noEmit",
		"test": "vitest --run --root ../.. $(find ../../packages/mcp -name '*.test.ts' | sort)"
	},
	"dependencies": {
		"@macos-cua/core": "workspace:*",
		"@modelcontextprotocol/sdk": "^1.0.0",
		"zod": "4.4.3"
	},
	"devDependencies": {
		"typescript": "^5.7.0",

		"vitest": "^2.1.0",
		"@types/node": "^22.0.0"
	}
}
